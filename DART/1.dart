void main() {
  Set mySet = {10,20,30,40,"i am a dart"};

  // mySet.add(20);
  // mySet.addAll({500, 1000});

  // mySet.remove(20);
  // mySet.removeAll({500, 1000});

  // mySet.removeWhere((mySet) => mySet % 2 == 0);

  mySet.first;
 mySet.last(mySet);
  mySet.length;

  print(mySet);
}
